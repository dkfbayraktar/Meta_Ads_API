name: Daily Meta Ads Data Fetch

on:
  schedule:
    - cron: '30 5 * * *' # UTC 05:30 = Türkiye saatiyle 08:30
  workflow_dispatch:

jobs:
  fetch-data:
    runs-on: ubuntu-latest

    steps:
    - name: Kodları checkout et
      uses: actions/checkout@v3

    - name: Python ortamını hazırla
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Bağımlılıkları yükle
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Veri çekimini çalıştır
      run: |
        python app.py &
        sleep 10
        curl http://127.0.0.1:5002/daily-insights
        curl http://127.0.0.1:5002/hourly-insights

    - name: Değişiklikleri git'e ekle ve push et
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@users.noreply.github.com"
        git add data/daily/*.json data/hourly/*.json || echo "No data to add"
        git commit -m "🔄 Günlük veri güncellendi - $(date +'%Y-%m-%d')" || echo "No changes to commit"
        git push || echo "Nothing to push"

